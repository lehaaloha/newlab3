name: Deploy to Render
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Test from lab3 folder
      run: |
        cd lab3
        python -c "print('Тест из папки lab3')"
